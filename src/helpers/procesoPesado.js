export const procesoPesado = (iteraciones) => {
    console.log(`Esto representa un proceso pesado con ${ iteraciones } iteraciones`)
    return `Esto representa un proceso pesado con ${ iteraciones } iteraciones`
};